# Shopmonkey API key (Bearer token from Settings > API in Shopmonkey)
SHOPMONKEY_API_KEY=

# Base URL — defaults to public production endpoint.
SHOPMONKEY_BASE_URL=https://api.shopmonkey.cloud/v3

# Polite delay between requests in milliseconds (default 250).
REQUEST_DELAY_MS=250
